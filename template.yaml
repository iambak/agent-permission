AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Agent Permission API

Globals:
  Function:
    Timeout: 15
    MemorySize: 128
    Runtime: python3.9

Parameters:
  Environment:
    Type: String
    Default: dev
    AllowedValues: [dev, prod]

Resources:
  # S3 Bucket for permissions data
  PermissionsBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub "agent-permissions-data-${Environment}-${AWS::AccountId}"
      VersioningConfiguration:
        Status: Enabled
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true

  # Lambda function for permissions
  AgentPermissionFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "agent-permission-api-${Environment}"
      CodeUri: src/
      Handler: lambda_handler.handler
      Environment:
        Variables:
          S3_BUCKET_NAME: !Ref PermissionsBucket
          S3_FILE_KEY: permissions.json
          ENVIRONMENT: !Ref Environment
      Policies:
        - S3ReadPolicy:
            BucketName: !Ref PermissionsBucket
        - S3WritePolicy:
            BucketName: !Ref PermissionsBucket
      Events:
        GetUserExists:
          Type: Api
          Properties:
            Path: /users/{user_id}
            Method: get
            RestApiId: !Ref AgentPermissionApi
        CreateUser:
          Type: Api
          Properties:
            Path: /users
            Method: post
            RestApiId: !Ref AgentPermissionApi
        GetPermissions:
          Type: Api
          Properties:
            Path: /permissions/{user_id}
            Method: get
            RestApiId: !Ref AgentPermissionApi
        AddPermission:
          Type: Api
          Properties:
            Path: /permissions/{user_id}/agents
            Method: post
            RestApiId: !Ref AgentPermissionApi
        GetAllPermissions:
          Type: Api
          Properties:
            Path: /permissions
            Method: get
            RestApiId: !Ref AgentPermissionApi
        ClearUserPermissions:
          Type: Api
          Properties:
            Path: /permissions/{user_id}
            Method: delete
            RestApiId: !Ref AgentPermissionApi
        ClearAllPermissions:
          Type: Api
          Properties:
            Path: /permissions
            Method: delete
            RestApiId: !Ref AgentPermissionApi

  # Lambda function for user profiles
  UserProfileFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub "user-profile-api-${Environment}"
      CodeUri: src/
      Handler: profile_handler.handler
      Environment:
        Variables:
          S3_BUCKET_NAME: !Ref PermissionsBucket
          S3_PROFILES_FILE_KEY: user_profiles.json
          ENVIRONMENT: !Ref Environment
      Policies:
        - S3ReadPolicy:
            BucketName: !Ref PermissionsBucket
        - S3WritePolicy:
            BucketName: !Ref PermissionsBucket
      Events:
        ListProfiles:
          Type: Api
          Properties:
            Path: /profiles
            Method: get
            RestApiId: !Ref AgentPermissionApi
        CreateProfile:
          Type: Api
          Properties:
            Path: /profiles
            Method: post
            RestApiId: !Ref AgentPermissionApi
        GetProfile:
          Type: Api
          Properties:
            Path: /profiles/{user_id}
            Method: get
            RestApiId: !Ref AgentPermissionApi
        UpdateProfile:
          Type: Api
          Properties:
            Path: /profiles/{user_id}
            Method: put
            RestApiId: !Ref AgentPermissionApi
        DeleteProfile:
          Type: Api
          Properties:
            Path: /profiles/{user_id}
            Method: delete
            RestApiId: !Ref AgentPermissionApi
        ProfileOptions:
          Type: Api
          Properties:
            Path: /profiles
            Method: options
            RestApiId: !Ref AgentPermissionApi
        ProfileUserOptions:
          Type: Api
          Properties:
            Path: /profiles/{user_id}
            Method: options
            RestApiId: !Ref AgentPermissionApi

  # API Gateway
  AgentPermissionApi:
    Type: AWS::Serverless::Api
    Properties:
      Name: !Sub "agent-permission-api-${Environment}"
      StageName: !Ref Environment
      Cors:
        AllowMethods: "'GET,POST,PUT,DELETE,OPTIONS'"
        AllowHeaders: "'Content-Type,X-Amz-Date,Authorization,X-Api-Key,X-Amz-Security-Token'"
        AllowOrigin: "'*'"

Outputs:
  AgentPermissionApi:
    Description: "API Gateway endpoint URL"
    Value: !Sub "https://${AgentPermissionApi}.execute-api.${AWS::Region}.amazonaws.com/${Environment}/"

  AgentPermissionFunction:
    Description: "Lambda Function ARN"
    Value: !GetAtt AgentPermissionFunction.Arn

  UserProfileFunction:
    Description: "User Profile Lambda Function ARN"
    Value: !GetAtt UserProfileFunction.Arn

  PermissionsBucket:
    Description: "S3 bucket for permissions data"
    Value: !Ref PermissionsBucket